# AUDIT_STATUS.md — Live Remediation Tracker

> Updated after every meaningful step. Statuses: `NOT STARTED` · `INVESTIGATING` · `IMPLEMENTING` · `TESTING` · `VERIFIED` · `BLOCKED` · `NEEDS FOLLOW-UP` · `WONT FIX` (justified only).
> Source of truth for findings: `AUDIT_REFERENCE.md`.

| ID | Severity | Finding | Status | Files Changed | Tests | Verification | Notes |
|----|----------|---------|--------|---------------|-------|--------------|-------|
| C1 | Critical | Ad Studio FK violation (`ad-studio` store) | VERIFIED | schema.prisma (AdVideo.data); migrations/0009_ad_video_data; api/src/index.ts (runtime ALTER); api/src/utils/storage.ts (export putS3Object+getApiBase; **fixed latent path-style S3 bug `/{bucket}{key}`→`/{bucket}/{key}`**); api/src/utils/ad-render.ts (storeAdResult decoupling, field-select in job); api/src/routes/ads.ts (download route + AD_VIDEO_FIELDS selects); integration/ad-render-storage.test.ts (new) | 8 new tests (DB path, S3 path w/ live mock endpoint, partial-config degrade, download 200/404×2, list/detail select-exclusion ×2) | tsc clean; full suite 67/67 (was 59) | Root cause: ads routed through store-media pipeline requiring a Store FK. Extra root cause found by testing: `signS3Request` built path-style URLs as `/{bucket}{key}` (missing slash) — every path-style R2/S3 PUT would have failed; existing round-trip test never asserted the URL path. Compatibility: live DB gets `data` via runtime ALTER; no Media rows created for ads; dashboard unchanged (absolute videoUrl both paths). Remaining risk: DB-fallback blobs still burn Neon on view (accepted; R2 is the advised path — M-video covers Range support). Verified 2026-08-25. |
| C2 | Critical | api-config drops MOMO_/ELEVENLABS_/STORAGE_ keys | VERIFIED | api/src/routes/api-config.ts (ALL_KEYS +14 keys with rationale comments) | 4 new tests: PUT round-trip of all 14 keys, GET returns them + allowlist filter includes them, unknown key still rejected, RETAILER role 403 | tsc clean; full suite 71/71 | Root cause: hardcoded ALL_KEYS allowlist (drives both GET filter and PUT filter) predated the Ad Studio/MoMo/Storage dashboard fields. Dashboard `save()` writes `<PREFIX>_ENABLED` toggles via /settings (separate endpoint) — unaffected. Remaining risk: none for this finding; secrets still returned unmasked by GET (that is H9, tracked separately). Verified 2026-08-25. |
| C4 | Critical | Subscription replay / non-transactional confirm | VERIFIED | api/src/routes/subscriptions.ts (claimSubscriptionPayment CAS + finalizeSubscriptionPayment $transaction + replay guards + callback log gating + webhook dashed-UUID parse fix); integration/subscription-replay.test.ts (new, 7 tests); subscriptions-manual.test.ts (confirm tests updated to CAS shape) | 7 new replay/integrity tests: apply-once, concurrent-loser, already-PAID short-circuit, mid-tx failure → 500, webhook full-UUID parse, webhook CAS-0 replay, bad signature 401; full suite 79/79 | tsc clean; suite green | Root cause: verify/confirm/webhook did blind `update(PAID)` + non-transactional activation; replays re-extended nextBillingDate (free-subscription bug). Fix: DB-level compare-and-set (`updateMany WHERE status != 'PAID'`, count===1) inside `$transaction` with activation; replays acknowledged idempotently with zero writes. DISCOVERED+FIXED: Flutterwave webhook parsed subscriptionId via `split('-')[1]` — truncated dashed UUIDs so the webhook could never find its payment; now `slice(4, lastIndexOf('-'))`. Invariant proven by tests: *a replayed event grants no additional entitlement*. Remaining risk: order-payment path `payments.ts markPaid` non-transactional (separate Medium finding, untouched here). Verified 2026-08-25. |
| C5 | Critical | OAuth tokens in query params | VERIFIED | api/src/routes/auth.ts (fragment redirect via URLSearchParams); storefront/src/app/auth/callback/page.tsx (readAndScrubTokens: fragment-first, legacy-query fallback, history.replaceState strip BEFORE any await); integration/oauth-callback.test.ts (new, 5 tests) | 5 adversarial tests: success → fragment-only (query asserted clean), JWT encoding integrity via URLSearchParams round-trip, token-failed error path clean, suspended-account path clean, forged state rejected pre-mint; full suite 84/84; storefront build green | tsc clean; suite green; SF build green | Root cause: `auth.ts:590` redirected with `?accessToken=&refreshToken=` → tokens in Vercel/server logs + browser history; page never cleaned URL. Fix: tokens move to `#fragment` (never sent to servers); page strips both fragment and legacy query immediately. Backward compatible both deploy orders. Note: dashboard `#token=` SSO handoff is H3 (separate); magic-link tokens-in-URL are inherent to email links (single-use, short TTL — accepted). Verified 2026-08-25. |
| C3 | Critical | No baseline migration | NEEDS FOLLOW-UP | packages/database/prisma/migrations/0000_init/migration.sql (new, 732 lines, 49 objects — full schema DDL generated by `prisma migrate diff --from-empty`) | Static chain proof: (1) 0000 is the exact current-schema DDL (prisma-generated, not hand-written); (2) scanned ALL of 0001–0009 — every CREATE is `IF NOT EXISTS`, the 2 bare `DROP NOT NULL` are Postgres no-ops when already nullable, zero ALTER-TYPE/SET-NOT-NULL/DML/DROP statements exist → the chain replays cleanly over 0000; (3) `prisma validate` 🚀 | **PENDING: live execution proof** — no Docker/Postgres in this environment. One-command proof on any throwaway Postgres (e.g. a free Neon branch): `DATABASE_URL="<branch-url>" npx prisma migrate deploy --schema packages/database/prisma/schema.prisma` then boot the API against it. | Deployment rule documented in the migration header: NEVER `migrate deploy` against the legacy production DB (plain CREATE TABLE would fail) — production continues on runtime drift-sync; if ever needed, `prisma migrate resolve --applied 0000_init` first. Fresh installs now work with plain `migrate deploy`. Verified statically 2026-08-25; execution proof outstanding. |
| H1 | High | Enforcer 'system' FK + partial failure | VERIFIED | api/src/jobs/subscription-enforcer.ts (per-iteration isolation via processLapsedSubscription + errors counter); api/src/index.ts (system actor upsert: passwordless, isActive=false → can never authenticate) | 4 new tests: early-failure sweep continues, post-suspension failure counted w/ suspension committed, missing-user → 'system' fallback works, clean sweep zero errors; full suite 88/88 | tsc clean; suite green | Root causes: (a) 'system' userId violated Notification/ActivityLog FKs; (b) no per-row try/catch → one bad row aborted the sweep AND could strand a store suspended-without-notice. Fix: real system actor row (INSERT .. ON CONFLICT (id) DO NOTHING at boot, guarded sub-try/catch so an email-collision can't abort migrations) + per-iteration error isolation. NOTE: system actor also fixes H2's root cause. Test-infra lesson recorded: clearAllMocks does NOT clear once-queues/rejected implementations — reset polluted mocks in beforeEach. Verified 2026-08-25. |
| H2 | High | Callback audit loss ('system' logActivity) | VERIFIED | (no new code — root cause fixed by H1's system actor); subscription-replay.test.ts (+audit-write assertion) | Webhook test now asserts `activityLog.create` is called with `{ userId: 'system', action: 'subscription:webhook_paid' }`; suite green | Root cause: ActivityLog.userId FK violation swallowed by logActivity's catch → audit records silently dropped. Fixed by H1's passwordless system actor. Decision (documented): keep actor='system' for automated webhook/renewal events — semantically correct; resolving the retailer's user id would cost an extra query per callback for no audit benefit. Remaining risk: logActivity still swallows ALL errors (console.error only) — acceptable; a durable audit queue is out of scope. Verified 2026-08-25. |
| H3 | High | #token= fixation, parsed during render | VERIFIED | packages/web/src/jwt.ts (isAcceptableHandoffToken gate: structure+role+exp with 30s skew; unconditional URL strip); retailer-dashboard/src/lib/auth.tsx + developer-dashboard/src/lib/auth.tsx (capture moved from render into useEffect); web/src/jwt.test.ts (new, 8 tests) | 8 adversarial tests: valid handoff accepted, expired-token fixation REJECTED (victim session intact + URL stripped), junk/malformed rejected, missing-claim rejected, non-token hash stripped, no-hash no-op, clock-skew, base64url padding; both dashboard builds green; API suite 88/88 | Root cause: `captureTokenFromUrl()` ran during render and wrote ANY `#token=` value into localStorage → crafted links silently replaced sessions. Residual risk (documented): fragment handoff cannot authenticate its ORIGIN — a valid, unexpired attacker token is still accepted; full fix = one-time exchange-code flow (server change, deferred as NEEDS FOLLOW-UP candidate). Deploy chain verified: dashboard `prebuild` rebuilds @nexus/web before next build. Verified 2026-08-25. |
| H4 | High | Refresh always-on + no single-flight | VERIFIED | packages/web/src/api.ts (hasSession gate, single-flight refreshAccessToken, shared handle401 for request+upload, /login loop guard, clearSession on dead refresh) | 5 tests: guest 401 → zero refresh calls + no redirect; refresh-once + retry with new token; 3 concurrent 401s → exactly 1 refresh call; dead refresh clears session + no /login loop; upload parity; web tsc clean | Severity corrected during investigation: `/auth/refresh` does NOT rotate the refresh token (auth.ts:259-263 reuses the session) — concurrent refreshes were wasteful (N DB session lookups per burst = Neon burn), not destructive. Behavior trade-off (documented): cookie-only sessions (localStorage cleared, httpOnly cookie alive) now treated as guests on 401 — invisible to JS by design; SSO/login flows set localStorage so main flows unaffected. Verified 2026-08-25. |
| H5 | High | Subscription guard fail-open | VERIFIED | retailer-dashboard/src/lib/subscription-state.ts (new pure decision fn); retailer-dashboard/src/lib/subscription-guard.tsx (subError state, fail-closed lock card with Retry, exempt /subscription preserved) | 6 tests on the pure decision fn: unknown-state locks (the H5 rule), ACTIVE opens, unexpired trial opens, expired trial locks, SUSPENDED/CANCELLED lock, null+no-error opens; dashboard build green | Root cause: `.catch(() => setSub(null))` made any API failure look like "no subscription" → `locked=false`. Fix: distinct error state → locked + Retry (fail closed). Server-side enforcement (requireActiveSubscription on orders) was always the real gate — this fixes the UI promise. Verified 2026-08-25. |
| H6 | High | Media base64 N+1 (no select) | VERIFIED | api/src/routes/media.ts (MEDIA_LIST_SELECT metadata-only select; response built from selected rows) | 2 tests: list select excludes `data` (asserted on the actual query args) + response rows blob-free; non-owner 403; full suite 90/90 | Root cause: `findMany` fetched ALL columns incl. base64 `data`, stripped only in JS afterwards — bytes already crossed DB→API. Impact: 200×5MB assets ≈ 1GB per page load → now KBs. Follow-up (noted): UI pagination/take limit deliberately NOT added — would hide assets without dashboard pagination support (separate UX task). Verified 2026-08-25. |
| H7 | High | Mirror staleness / non-atomic restore | VERIFIED | api/src/utils/db-mirror.ts (pg_stat change counters in digest; lazy DATABASE_URL_FALLBACK read; transactional restore with SET LOCAL); api/src/index.ts (loud fallback-mode warning); integration/db-mirror.test.ts (new, 7 tests) | Tests: digest sees UPDATE-only changes → re-mirror; unchanged counts+counters → skip; legacy snapshot (no changes key) → re-mirror; restore runs in ONE transaction with SET LOCAL (pool-safe, FK auto-restored); mid-restore failure rejects atomically; skip when target has data; digest shape | Root causes fixed: (1) row-count digest missed UPDATEs → stale fallback served as truth; (2) non-transactional restore + once-only hasData() skip = permanently half-restored fallback; (3) pool poisoning via session-scoped SET. Accepted-risk (documented): writes made while serving FROM fallback are still not reconciled back — full bidirectional sync is a protocol-level project; boot now warns loudly. Bonus: lazy env read fixes test/runtime config ordering. Verified 2026-08-25. |
| H8 | High | Backups unbounded in settings row | VERIFIED | api/src/routes/backups.ts (dump policy excludes media+sessions; retention cap newest-5 via BACKUP_RETENTION 1-20; 25MB size guard → 413 with R2 guidance; create returns metadata only); integration/backups.test.ts (new, 4 tests) | 4 tests: dump excludes media/sessions + includes core tables + metadata-only response; retention drops oldest beyond limit (dropped count surfaced); oversized snapshot → 413 with no settings write; download returns full payload; full suite 101/101 | Root causes: media base64 blobs dominated dumps; unbounded stacking in one jsonb row (each create re-read+rewrote everything; >1GB hard-fails). Trade-off documented: restores no longer include media blobs or sessions (sessions are ephemeral; media → use R2 path for full fidelity). CU note: this was burner #5 — each backup was a full-DB read + monster-row write. Test-infra note: root proxy auto-creates models for `$`-methods — must assign vi.fn explicitly. Verified 2026-08-25. |
| H9 | High | GET /settings returns secrets unmasked | VERIFIED | api/src/utils/secrets.ts (new: isSecretKey/maskValue/maskSettingsForRole/isMaskPlaceholder); api/src/routes/settings.ts (role-based masking); api/src/routes/api-config.ts (masking + mask-echo guard on PUT) | 5 tests incl. adversarial: DEVELOPER masked / SUPER_DEVELOPER raw on both endpoints; public keys (PUBLIC_KEY, base URLs, merchant code) stay raw; **echoed mask never overwrites the stored secret on Save All**; owner PUT still writes real values; suite 106/106 | Root cause: both GETs returned every raw value to plain DEVELOPER (incl. payment secrets AND the database_backups dumps via settings). Companion fix is the critical one: masking without the PUT guard would have let a dashboard "Save All" silently replace real secrets with `••••last4`. Owner workflow unchanged (SUPER_DEVELOPER sees raw). Verified 2026-08-25. |
| H10 | High | Dockerfile.api broken (npm ci + build order) | VERIFIED | Dockerfile.api (rewritten: all 7 workspace manifests + schema pre-COPY, dependency-order build shared→database→api, explanatory header); .dockerignore (new: node_modules/.git/.next/dist/.env excluded — secrets never bake in) | Locally proven: exact build chain `shared → database → api` runs green; `packages/api/dist/index.js` (CMD target) produced; lockfile contains all workspace manifests; suite 106/106. BONUS FIX: `npm run build -w packages/api` was ALREADY broken pre-audit (untyped `req` in api-config PUT → TS2339) — masked because production runs tsx (no emit); typed as AuthRequest, build now green | Remaining risk: literal `docker build` executes only when Render flips to Docker (no Docker in this env) — every internal claim individually verified. Deployment note: Render → API service → Runtime=Docker, Dockerfile path ./Dockerfile.api. Verified 2026-08-25. |
| H11 | High | Store theme leak (no unmount cleanup) | VERIFIED | storefront/src/components/StoreShell.tsx (snapshot/restore effect + data-store-theme ownership marker); storefront/src/lib/theme.tsx (ThemeProvider defers while a store owns the theme) | Storefront build green; logic reviewed (snapshot before override, cleanup restores persisted data-theme + removes inline vars; store→store transitions restore-then-reapply) | Root cause: store wrote data-theme + 6 CSS vars to documentElement with no cleanup → leaked into the whole main site until reload. EXTRA defect found: child effects run BEFORE parent effects, so ThemeProvider's mount effect stomped the store's data-theme on first paint (they fought). Fix: explicit ownership marker; ThemeProvider skips while a store owns the look (toggle still records the preference for after). Verified 2026-08-25. |
| H12 | High | Cross-store cart contamination | VERIFIED | storefront/src/components/ClientProviders.tsx (removed global 'adorn' seeding); cart/page.tsx + checkout/page.tsx (single source: localStorage → URL slug → none; no invented fallbacks; empty-state links degrade to /shop) | Storefront build green; grep-audited remaining storeSlug uses (only render with non-empty cart = real context) | Root cause: every visitor was silently seeded to demo store 'adorn', and cart vs checkout had DIVERGENT fallbacks ('adorn' vs 'shop') → a cart built against store A could check out against store B. Fix: store context comes only from explicitly visiting a store (StoreShell) or the header switcher. By-design note: carts are store-scoped — switching stores shows that store's cart (header switcher makes context visible). Verified 2026-08-25. |
| H13 | High | Silent money-action failures (orders page) | VERIFIED | retailer-dashboard/src/app/orders/page.tsx (busyId + try/catch + error banner + confirm on Cancel, table AND detail-modal buttons disabled while busy); retailer-dashboard/src/app/subscription/page.tsx (subscribing/cancelling busy states on payment-initiating buttons) | Retailer dashboard build green; pattern matches the audited gold-standard dev subscriptions page | Root cause: `updateStatus` had no try/catch (failed status changes looked successful), no busy state (double-fire), and Cancel had no confirm; subscription Activate/Cancel lacked disabled states (double-click = two mobile-money intents). Verified 2026-08-25. |
| H14 | High | Missing FK/hot-path indexes | VERIFIED | schema.prisma (@@index on OrderItem.orderId/productId, Cart storeId+customerId & storeId+sessionId, Media storeId+createdAt, Session userId+isActive, Review productId+createdAt, SubscriptionPayment subscriptionId+createdAt, AnalyticsEvent ×4); migrations/0010_hot_path_indexes (12 indexes, Prisma-convention names); api/src/index.ts runMigrations (same 12 via CREATE INDEX IF NOT EXISTS — live DB covered without migrate deploy) | db:generate OK (schema valid); api tsc clean; suite 106/106; index names self-consistent between migration and runtime paths | Impact: Postgres does not auto-index FKs — order lookups, cart lookups (pre-cart-action), media lists, product reviews, session deactivation, analytics reports all seq-scanned; cost grows linearly with data (CU burner #10). Boot note: first boot creates indexes non-concurrently (brief lock) — at current scale fine; CONCURRENTLY available if tables grow large. Verified 2026-08-25. |
| M-money | Medium | Float money; Payment.currency default USD | VERIFIED | schema.prisma (Payment.currency default USD→UGX); migrations/0011_payment_currency_default (definition-only ALTER); api/src/index.ts (drift-sync equivalent for live DB) | db:generate OK; tsc clean; suite 131/131; git diff verified ONLY the currency line changed (a stray failUrl column was accidentally introduced and reverted before commit) | Float→Int reviewed and ACCEPTED as-is: UGX is integral, coupon discounts round to whole units (utils/coupon-discount.ts) → Float64 exact for this platform; a full Int/Decimal refactor would touch every consumer for zero numeric gain (documented decision, revisit only if multi-currency arrives). Existing USD-defaulted rows untouched — definition-only change. Verified 2026-08-25. |
| M-killswitch | Medium | Kill-switch fail-open on missing table | VERIFIED | api/src/middleware/kill-switch.ts (15s in-memory cache, serve-stale-on-error, loud fail-open only when never read; TTL=0 in test env); api/src/routes/kill-switch.ts (invalidate cache on PUT for immediate effect) | tsc clean; suite 106/106 (test TTL=0 keeps per-test mocks deterministic) | Two fixes in one: (CU burner #1) per-request killSwitch.findFirst → ~4 queries/min platform-wide; (fail behavior) query errors now serve last-known state instead of silently disabling all switches exactly when degraded; brand-new deploys with no row still fail open, loudly. Emergency PUT takes effect immediately via invalidation. Remaining: fallback DB missing the table entirely → serves stale cache then fails open (documented; mirror now carries kill_switch). Verified 2026-08-25. |
| M-mirror | Medium | Mirror dump single-row + unsafe exec | PARTIAL | db-mirror.ts (H7: atomic restore + change detection — already shipped); media blobs remain in mirror intentionally for fallback fidelity | H7 changes shipped | Structural: single-row JSONB limit (~1GB) persists by design — removing media would break fallback-mode image serving. Mitigation path: migrate blobs to R2 (zero-egress), which shrinks the mirror automatically. H7's change detection already reduces update frequency. Documented as accepted structural constraint pending R2 migration. Reviewed 2026-08-25. |
| M-prune | Medium | No pruning (analytics/logs/notifications/sessions) | VERIFIED | api/src/jobs/retention.ts (new: daily sweeps, per-sweep isolation, env-tunable day thresholds, RETENTION_DISABLED kill); api/src/index.ts (boot + 24h schedule, overlap-guarded); integration/retention.test.ts (4 tests) | 4 tests: all sweeps run with counts, notifications prune only isRead, one failure doesn't abort others, disabled skips everything; suite green | Impact: bounds the forever-growing tables (CU burn compounding). Thresholds: analytics 90d, activity_logs 365d (audit trail), read notifications 30d, dead/expired sessions 7d — all env-tunable. Verified 2026-08-25. |
| M-csp | Medium | CSP unsafe-inline + localStorage JWTs | VERIFIED | storefront/middleware.ts + retailer-dashboard/middleware.ts + developer-dashboard/middleware.ts (nonce-based CSP via CSP_NONCE env flag + strict-dynamic); all 3 next.config.js (static CSP conditional — skipped when CSP_NONCE set) | All 3 builds green | Env-gated: CSP_NONCE unset = current static CSP (zero risk); CSP_NONCE=true = per-request UUID nonce + strict-dynamic, script-src loses unsafe-inline. Middleware skips static assets. Recommended rollout: enable in staging first, verify hydration + RSC flight payloads, then production. Verified 2026-08-25. |
| M-video | Medium | No Range support; buffered MP4; public docs | VERIFIED | api/src/utils/range.ts (new serveRangeBuffer); api/src/index.ts (/uploads handler now serves 206 + 416); api/src/routes/ads.ts (download route uses the same helper); integration/range.test.ts (7 tests) | 7 tests: full 200 + Accept-Ranges, 206 partial with Content-Range, open-ended, suffix, 416 unreachable-range cases, ads download partial + full-cache headers; suite 131/131; tsc clean | Frames video now plays on iOS Safari and seekable everywhere. Public capability-URL for documents kept by design (UUID unguessable, nosniff enforced server-side, consistent with /uploads); R2 public URLs remain the recommended serving path. Verified 2026-08-25. |
| M-sigterm | Medium | No SIGTERM handler; stuck RENDERING rows | VERIFIED | api/src/index.ts (SIGTERM/SIGINT graceful shutdown: close listener → 10s grace → disconnect → exit); api/src/utils/ad-render.ts (recoverStuckAdRenders: requeue + re-run on boot); api/src/integration/ad-recovery.test.ts (3 tests) | 3 tests: stuck rows requeued + re-run, none-stuck no-op, recovery failure swallowed (boot-safe); tsc clean; suite 113/113 | Root cause: Render's deploy SIGTERM hard-killed the process — in-flight renders stuck in RENDERING forever with no retry path. Fix: graceful shutdown + boot-time recovery. Verified 2026-08-25. |
| M-ssrf | Medium | Ads captureUrl SSRF; batch unbounded | VERIFIED | api/src/utils/url-guard.ts (new: private-range v4/v6/CGNAT/metadata checks, dangerous-hostname blocklist, DNS resolution verifying EVERY address — rebinding defense, unresolvable = reject); api/src/utils/ad-render.ts (validate before launch + Playwright route interception aborting internal/non-http requests mid-navigation, covering redirects); api/src/routes/ads.ts (validate at POST / and /batch doors; MAX_BATCH=10); integration/url-guard.test.ts (7 tests, deterministic mocked DNS) | 7 tests: private v4/v6/metadata/CGNAT flagged, public IPs allowed, dangerous hostnames, protocol+credential rejection, literal private targets rejected without DNS, unresolvable rejected; suite 120/120 | Root cause: headless Chrome fetched any URL on the API host. Defense in depth: door validation (routes) + job re-validation + per-request interception (redirect coverage). Residual (documented): DNS-rebinding between check and fetch is theoretical (short window, DEVELOPER-gated endpoint); Playwright interception re-checks every request host. Test-infra: DNS mocked — real resolvers hijack NXDOMAIN. Verified 2026-08-25. |
| M-timing | Medium | Flutterwave compare not timing-safe | VERIFIED | api/src/utils/crypto.ts (new safeEqual: timingSafeEqual + constant-time length-mismatch path, no length oracle); api/src/routes/subscriptions.ts:309 (webhook signature now uses it) | 4 tests (equal/different/length-mismatch/unicode); tsc clean; suite 124/124; existing bad-signature→401 test still guards the route | Plain `!==` leaked prefix-match timing to webhook probers; now constant-time. Verified 2026-08-25. |
| M-deps | Medium | prisma CLI/client mismatch; multer 1.x; tsx prod | VERIFIED | packages/database/package.json (prisma ^5.10.0→^5.22.0 — CLI aligned with client); packages/api/package.json (multer ^1.4.5-lts.1→^2.2.0 — 1.x DoS CVEs fixed, memoryStorage API unchanged); root package.json (tsx moved dependencies→devDependencies) | db:generate OK on prisma 5.22; suite 131/131 on multer 2.x; git diff verified only the 3 manifests + lockfile changed | Was blocked on flaky network (ECONNRESET to registry); owner ran installs on a stable connection. Verified 2026-08-26. |
| M-authmodels | Medium | 3 divergent auth trust models | VERIFIED | retailer-dashboard/src/lib/auth.tsx + developer-dashboard/src/lib/auth.tsx (JWT instant-paint then /auth/me reconciliation — storefront's model); developer layout roles verified ALREADY correct (audit sub-finding inaccurate — documented) | Both dashboard builds green | Root cause: dashboards trusted client-side decodeJwt alone — an expired-but-decodable token showed a "logged in" UI (with empty names) until the first 401. Now: server-verified profile hydrates immediately; 401 on /auth/me flows through the H4-fixed api client (session cleared, redirect). Audit correction recorded: developer layout DID pass roles — per-page re-checks were redundant, not missing. Verified 2026-08-25. |
| M-categories | Medium | Categories GET performs writes | VERIFIED | api/src/routes/categories.ts (GET is now read-only: single findMany; seed-if-empty for fresh stores kept; tree-sync removed from hot path — remains exported as an explicit drift-repair utility) | tsc clean (also caught a retention.ts typing slip); suite 110/110 | Impact: every storefront category view ran the full jiji-tree walk (dozens of sequential queries) + a second full fetch. Reads are pure now; ~1 query per view. Drift repair stays available via explicit call/reseed. Verified 2026-08-25. |
| M-img | Medium | Image remotePatterns mismatch | VERIFIED | storefront/next.config.js (remotePatterns → https hostname '**' with rationale) | Storefront build green (Next validates config at build) | Root cause: dashboards allowed all hosts, storefront whitelisted 3 → retailer-pasted logos/product images from any other host threw "Invalid src" on the storefront. User-supplied URLs are core platform behavior; CSP img-src already permits https: platform-wide. Verified 2026-08-25. |
| M-grid | Medium | minmax(400px) overflow on settings pages | VERIFIED | retailer-dashboard/src/app/settings/page.tsx + developer-dashboard/src/app/settings/page.tsx (minmax(min(400px,100%),1fr) — same fix pattern as the product-grid phone fix) | Both dashboard builds green | Same overflow class as the Phase-0 phone fix: grids forced 400px columns on narrow phones → horizontal push. Verified 2026-08-25. |
| L-magicidx | Low | magic_link_tokens email index missing in migrations | ACCEPTED | schema.prisma:110 already has `@@index([email])` | — | Audit sub-finding inaccurate; index exists. Verified 2026-08-25. |
| L-verified | Low | isVerifiedPurchase always true | VERIFIED | api/src/routes/reviews.ts (now checks orderItem.findFirst for DELIVERED status) | Suite 131/131 | Verified 2026-08-25. |
| L-wishlist | Low | Wishlist ignores real product images | VERIFIED | storefront/src/app/wishlist/page.tsx (uses product.images?.[0] with logo fallback) | Storefront build green | Verified 2026-08-25. |
| L-membersince | Low | Member Since shows today | VERIFIED | storefront/src/app/account/page.tsx (uses user.createdAt from /auth/me) | Storefront build green | Verified 2026-08-25. |
| L-urlenc | Low | Search query not URL-encoded | VERIFIED | storefront/src/components/Header.tsx + StoreHeader.tsx (encodeURIComponent) | Storefront build green | Verified 2026-08-25. |
| L-slug | Low | Slug check per keystroke, no abort | VERIFIED | storefront/src/app/create-store/page.tsx (400ms debounce + cleanup) | Storefront build green | Verified 2026-08-25. |
| L-dates | Low | Date format drift | ACCEPTED | — | — | Cosmetic: platform Uganda-focused; en-US consistent across pages. Verified 2026-08-25. |
| L-ann | Low | Announcements unbounded growth | VERIFIED | api/src/routes/announcements.ts (cap at 100, returns 400 when exceeded) | Suite 131/131 | Verified 2026-08-25. |
| L-role | Low | Role-mismatch → /login instead of no-access | VERIFIED | packages/web/src/auth-guard.tsx (shows Access Denied overlay for wrong role; /login only when unauthenticated) | All 4 builds green | Verified 2026-08-25. |

---

## Remediation log (chronological)

- **Program started** — reference files created. No code changed yet.
